name: camel_eval
version: "1.0.0"
profile: camel

model-paths: ["models"]
seed-paths: ["seeds"]

vars:
  raw_parquet_path: "../data/raw/train.parquet"

models:
  camel_eval:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table
